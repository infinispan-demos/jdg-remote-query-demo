/* @Indexed */
message Task {

   /* @Field() */
   required string title = 1;

   /* @Field(index=Index.NO, store=Store.NO) */
   required int64 id = 2;
   
   optional fixed64 completedOn = 3;

   optional fixed64 createdOn = 4;

   optional bool done = 5;   
}